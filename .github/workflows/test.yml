name: Node.js CI

on:
  push:
    branches: [ 'feature/lerna-out' ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [ 16.x ]
    steps:
      - run: npm run test
